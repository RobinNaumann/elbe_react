{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prep": "npm remove elbe-ui && rm -rf node_modules/.vite && npm install -f --install-links file:../"
  },
  "dependencies": {
    "elbe-ui": "file:..",
    "preact": "^10.24.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "typescript": "^5.8.2",
    "vite": "^4.3.2"
  }
}
