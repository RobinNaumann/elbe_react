{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prep": "npm remove elbe-ui && rm -rf node_modules/.vite && npm install -f --prefer-offline --install-links file:../"
  },
  "dependencies": {
    "elbe-ui": "file:..",
    "history": "^5.3.0",
    "preact": "^10.24.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "typescript": "^5.8.2",
    "vite": "^4.3.2"
  }
}
